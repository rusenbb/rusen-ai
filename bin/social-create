#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

exec node --no-warnings=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types "$ROOT_DIR/scripts/social-create.ts" "$@"
